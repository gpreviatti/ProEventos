apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: 'pro-eventos'
spec:
  type: ClusterIP
  selector:
    app: backend-service
  ports:
    - name: "http"
      port: 5000 #service
      targetPort: 80 #pod
