apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: pro-eventos
spec:
  selector:
    app: db-deployment
  ports:
    - name: db-service
      port: 1433 #service
      targetPort: 1433 #pod
  type: ClusterIP
